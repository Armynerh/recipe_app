<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <% if user_signed_in? %>
  <div class= "flex">
<div>Welcome <%= current_user.name %></div>
  <div>
  <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } %>
  </div>
  </div>
<% end %>
<nav>
    <ul>
      <li><%= link_to 'Home', root_path %></li>
      <li><%= link_to 'Foods', foods_path %></li>
      <li><%= link_to 'Public Recipes', public_recipes_path %></li>
<li><%= link_to 'General Shopping List', general_shopping_lists_path %> </li>     <!-- Add other navigation links as needed -->
    </ul>
  </nav>
    <%= yield %>
  </body>
</html>
